components:
  parameters:
    Password:
      in: header
      name: Password
      description: _optional_ - 驗證用密碼 (當Name跟Password都有給時，會驗證使用者身分)
      required: false
      schema:
        type: string
        example: 'rocks'